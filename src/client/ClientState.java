package client;

/**
 * Enum representing the state of a client connection.
 * This enum is used to indicate whether the client is connected or closed.
 * @version 1.0
 */
public enum ClientState {
    CLOSED,
    CONNECTED
}